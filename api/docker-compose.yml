services:
  postgres:
    image: postgres:17
    container_name: buni-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: buni
    ports:
      - "5432:5432"
