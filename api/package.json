{
  "name": "buni",
  "version": "1.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "db:generate": "bun --env-file .env --bun drizzle-kit generate",
    "db:migrate": "bun --env-file .env --bun drizzle-kit migrate",
    "db:push": "bun --env-file .env --bun drizzle-kit push",
    "db:reset": "bun --env-file .env src/database/reset.ts",
    "db:studio": "bun --env-file .env --bun drizzle-kit studio"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.1",
    "@elysiajs/openapi": "^1.4.13",
    "aws4fetch": "^1.0.20",
    "better-auth": "^1.4.17",
    "drizzle-orm": "^0.45.1",
    "drizzle-seed": "^0.3.1",
    "elysia": "1.4.22",
    "pg": "^8.17.2",
    "resend": "^6.9.2",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0",
    "bun-types": "latest",
    "drizzle-kit": "^0.31.8"
  },
  "module": "src/index.js"
}